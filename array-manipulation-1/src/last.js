/* exported last */
// define a new function of last with a parameter of array
// in the function, return the value of the array at the array.length - 1 index, to find the last value of the array

function last(array) {
  return array[array.length - 1];
}
