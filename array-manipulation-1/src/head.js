/* exported head */

// define function called head and pass array as an argument
// inside of the function return the value of the array at the index of 0

function head(array) {
  return array[0];
}
